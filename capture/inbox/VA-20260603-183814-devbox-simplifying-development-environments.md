# Devbox: Simplifying Development Environments

**Source:** https://youtu.be/U7YDcqP0dlI?si=--J7eUtxihUbw2D9
**Extracted:** 2026-06-03T18:38:14.005610
**Tool:** Brain Console → Research Orchestrator → NotebookLM

## Transcript

your readme is lying to you it says setup takes five minutes then node is wrong python is wrong postgress is missing docker is taking forever and now we're debugging before we even get started your dev environment should not live in a readme it should live in Git that's what Devbox does one devbox JSON file one command Devox shell same environment for every dev with no global installs and no Nyx knowledge required let me show you at first Devbox looks almost too simple you make a Devbox JSON you list the tools your project needs node Go Python Postgress whatever your stack actually needs you commit that file then anyone else can just run DevBox shell and they get the same environment you do same versions tools scripts no global installs no please install these eight things first no homebrew state from years ago your setup stops living in someone's memory it starts living in your repo now that sounds small but if you've ever lost half a day to a broken local setup you already know that this is not small if you enjoy coding tools to speed up your workflow be sure to subscribe we have videos coming out all the time now we're starting off here so I'm going to start with an empty project i'm going to make a new folder we'll call it Devbox demo we cd into the folder and all we need to do once we have Devbox is run DevBox in it devbox creates a file called Devbox JSON right now it's basically empty it's just the boilerplate that Devbox gives us now let's add the tools this project actually needs to add tools we can say devbox add and I'm going to install things like go NodeJS and some Python now here's the important part i'm not installing these globally i'm not changing my system node i'm not touching my system with Python these tools belong to this project now when I run DevBox shell and I'm inside a clean project environment now that you're in this environment you could just check the versions right go version no version Python version right i can check everything to make sure it's running now that's the big payoff the project asked for specific tools devbox gave me those tools now let's add a script and inside Devbox JSON we can actually define a test and and I'm just going to echo back these running tests and I'll get the node in the Go version when you run devbox run test and now that same command works for anyone using this repo same script same tools same environment now watch what happens when I leave you can just run exit you are going to leave that environment and I'm back to my normal machine environment so that was dead simple right like what is Devbox actually doing well Devbox uses Nyx under it all nyx is great because it's built for reproducibility instead of saying install whatever happens to be the latest today it can pin the exact tools your project actually needs now that is the good part the hard part is that Nyx can feel like a whole new world there are a lot of concepts that are great but not exactly friendly when all you wanted was the right node version devbox takes shows us something different here it says what if we kept the reproducibility but made the workflow feel normal so instead of writing nyx expressions you can use commands like devbox add devox search shell run services all of these commands are much more simpler and your project gets two important files you get a JSON file and a lock file think of the devbox JSON as what our environment needs think of the devbox lock file as the pin to pin exactly what you got you commit both now your environment is not just a paragraph in a readme file it's part of the actual project devbox works on Mac OS Linux and WSL it can integrate with VS Code it can define scripts it can manage services like databases and when you need to it can export to things like Docker Devont Containers and CI workflows the value of this isn't just cool tool right it's a dead simple tool the value is I think just time the first problem is the readme right the readme could say really anything it could say "Hey install node 18 but the app has changed it really needs node 20." The second problem this actually helps with is onboarding it's your first day on the job it should be easy to get going we know that's not the case right so you shouldn't need to ask which node version do I need you shouldn't need to ping someone hey which Python version are we on do I really need Postgress locally and why does this only work for little Timmy over there they should just have to clone the repo enter the shell and run the project if something breaks at least everyone is starting from the same environment the problem is global pollution trying tools should not wreck your laptop you want go 1.22 for this repo you add it you want node 20 here but something else elsewhere fine okay that's fine the tools live with the project your system stays cleaner with Devbox your environment definition can be shared between local development and automation does that fix every CI problem no it's not going to but it removes a huge class of dumb problems and dumb problems are the ones that hurt us the most they're simple we still have them all the time finally here is Docker heavy local Docker workflow more specifically docker is still great by far if you need containers use containers but a lot of teams use Docker locally because they don't have a better way to manage tools now what's good here is the workflow is dead simple devbox add shell run you don't have to learn that much the environment becomes a real part of your project a real file in the repo when everyone uses the same versions and scripts debugging gets easier but that's great super simple what's going to annoy you is well the first Nextoor download it took a while to download it's fine it's the first time okay JSON is simple but it can get ugly as we know if we add too much into it for basic scripts it's fine for complex setup logic don't cram a giant shell command in a JSON just put the logic in a SH file then call that from Devox and finally Devbox is not a full cloud IDE if you need browserbased coding instant preview URLs you may still want something like code spaces devbox is best at local and CI reproducibility devbox is not going to solve every development problem but it can solve the ones that annoy us the most which is really just getting the project to run so could be worth a shot especially if your project has more than one language or more than one CLI tool if you enjoy coding tools like this be sure to subscribe to the Better Stack channel we'll see you in another
