# Inbox — Failed

Failed, blocked, unreadable, duplicate-conflicted, or approval-blocked intake items live here after migration.

This folder is for preserving failure evidence without losing the original capture.

Compatibility note: legacy `capture/failed/` remains until Brain routing and validation switch to `inbox/failed/`.
