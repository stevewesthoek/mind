# Live Decisions

Current committed decisions that should guide future work.

## Accepted

- Obsidian is the primary human cockpit.
- `mind` uses clean unnumbered folders for the new structure.
- Legacy numbered folders remain untouched until validation and archive phase.
- Save-to-Mind keeps the public `/mind-inbox` webhook path for compatibility.
- The internal Save-to-Mind target changes to `capture/inbox/` only after the new structure exists and is validated.
- The model-router implementation belongs in `brain`; this vault stores the router contract and human memory.
- Brain Core owns machine/runtime state and safe actions.
